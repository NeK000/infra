[defaults]
host_key_checking = False
retry_files_enabled = False
roles_path = ./galaxy_roles:./roles:./submodules
inventory = ./hosts.yaml
remote_user = root

[privilege_escalation]
become_ask_pass = False

[git]
recursive = true # git submodules
