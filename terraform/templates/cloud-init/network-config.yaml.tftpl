version: 2
ethernets:
  ${interface_name}:
    dhcp4: false
    addresses:
      - ${ip_address}/${cidr_prefix}
    routes:
      - to: default
        via: ${gateway}
    nameservers:
      addresses:
%{ for server in nameservers ~}
        - ${server}
%{ endfor ~}
